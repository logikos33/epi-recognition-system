# EPI Monitor V2 — Railway deployment
# Note: ultralytics/torch excluded (2GB+, installed separately or lazy-loaded)
# YOLO health check returns 503 if not installed — graceful degradation

# Web Framework
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
werkzeug>=3.0.0

# WebSocket
python-socketio>=5.10.0
eventlet>=0.33.0

# Database
psycopg2-binary>=2.9.0

# Authentication
pyjwt>=2.8.0
bcrypt>=4.1.0

# HTTP
requests>=2.31.0

# Utils
python-dotenv>=1.0.0
gunicorn==21.2.0

# EPI Monitor V2 — Railway minimal deps
# SocketIO/eventlet removed — new arch uses sync gunicorn workers

# Web Framework
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# Database
psycopg2-binary>=2.9.0

# Authentication
pyjwt>=2.8.0
bcrypt>=4.1.0

# HTTP
requests>=2.31.0

# Utils
python-dotenv>=1.0.0
gunicorn==21.2.0

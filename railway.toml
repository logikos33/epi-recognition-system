[build]
builder = "NIXPACKS"

[deploy]
startCommand = "/opt/venv/bin/gunicorn wsgi:app --worker-class eventlet --workers 1 --worker-connections 1000 --timeout 120 --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
startCommand = "gunicorn --bind 0.0.0.0:5001 --workers 1 --timeout 120 api_server_full:app"

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

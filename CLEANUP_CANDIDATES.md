# Candidatos à remoção futura
# Verificar manualmente antes de apagar qualquer item desta lista.
# Nenhum arquivo foi removido automaticamente — apenas cache e temporários.

## Como verificar se um arquivo é seguro para remover:
# 1. grep -rn "nome_do_arquivo" . --include="*.py" --include="*.tsx" --include="*.jsx"
# 2. Se retornar 0 resultados fora do próprio arquivo: candidato à remoção
# 3. Confirmar com Vitor antes de apagar

## Arquivos identificados na auditoria acima:
# (preencher após análise dos resultados acima)

# EPI Monitor V2 — minimal Railway build
# No nixPkgs — avoid long nixpkgs downloads
# ffmpeg available on Railway by default or installable at runtime

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements-railway.txt"
]

[start]
cmd = "sh start.sh"

[phases.setup]
nixPkgs = ["python311", "ffmpeg", "libGL", "libGLU", "glib", "gcc"]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools wheel",
  "pip install -r requirements.txt"
]

[start]
cmd = "python railway_start.py"

# EPI Monitor V2 — modular Flask API
[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements-railway.txt"
]

[start]
cmd = "gunicorn wsgi:app --workers 2 --timeout 120 --bind 0.0.0.0:$PORT --log-level info"

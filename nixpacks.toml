[phases.setup]
nixPkgs = ["python311", "ffmpeg", "libGL", "libGLU", "glib"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "python3 railway_start.py"

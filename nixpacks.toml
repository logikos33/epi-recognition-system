[phases.build]
cmds = ["pip install --no-cache-dir -r requirements-api.txt"]

[phases.start]
cmds = ["python api_server.py"]

[start]
cmd = "python api_server.py"

[build]
cmd = "pip install --no-cache-dir -r requirements-api.txt"
